# Kubernetes Namespace for Profit First Application
# Namespaces provide isolation and organization for resources

apiVersion: v1
kind: Namespace
metadata:
  # Namespace name - all application resources will be deployed here
  name: profit-first
  labels:
    # Labels for identification and filtering
    name: profit-first
    environment: production
