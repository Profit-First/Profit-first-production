apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: profit-first
  labels:
    app: frontend
spec:
  type: LoadBalancer  # Changed from ClusterIP for direct access
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: frontend
