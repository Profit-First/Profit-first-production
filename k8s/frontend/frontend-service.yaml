apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: profit-first
  labels:
    app: frontend
spec:
  type: LoadBalancer
  ports:
  - port: 8084  # External port
    targetPort: 80  # Container port (nginx listens on 80)
    protocol: TCP
    name: http
  selector:
    app: frontend
